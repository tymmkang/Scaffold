# set(SCAF_EM_OUTPUT_DIR "${SCAF_OUTPUT_DIR}/CMakeExternal")
set(FETCHCONTENT_BASE_DIR "${SCAF_OUTPUT_DIR}/CMakeExternal")

file(GLOB CMAKE_IN_CURRENT_DIR "${CMAKE_CURRENT_LIST_DIR}/*.cmake")
foreach(CMAKE_FILE ${CMAKE_IN_CURRENT_DIR})
    include(${CMAKE_FILE})
endforeach(CMAKE_FILE)
